console.log("Phase 1 setup has no secrets, database, or local datasets to initialize.");
console.log(
  "Run `npm install`, copy `.env.example` to `.env.local` if needed, then run `npm run dev`.",
);
